#pragma once

namespace Config {
    // Render resolution (internal)
    constexpr int RENDER_WIDTH = 640;
    constexpr int RENDER_HEIGHT = 360;

    // Window resolution (scaled)
    constexpr int WINDOW_WIDTH = 1280;
    constexpr int WINDOW_HEIGHT = 720;
}